<p id="notice"><%= notice %></p>

<h1>Drivers</h1>

<table>
  <thead>
    <tr>
      <th>adı_soyadı</th>
      <th>dogum_tarihi</th>
      <th>tc_kimlik_no</th>
      <th>telefon</th>
      <th>adres</th>
    </tr>
  </thead>

  <tbody>
    <% @drivers.each do |driver| %>
      <tr>
        <td><%= driver.adı_soyadı %></td>
        <td><%= driver.dogum_tarihi %></td>
        <td><%= driver.tc_kimlik_no %></td>
        <td><%= driver.telefon %></td>
        <td><%= driver.adres %></td>
        <td><%= link_to 'Show', driver %></td>
        <td><%= link_to 'Edit', edit_driver_path(driver) %></td>
        <td><%= link_to 'Delete', driver, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Driver', new_driver_path %>
